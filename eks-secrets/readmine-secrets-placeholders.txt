REDMINE_DB_MYSQL=$(aws secretsmanager get-secret-value --secret-id readmine-rds-creds --region us-east-1 --query SecretString --output text | jq '."host"' | awk -F : '{print $1}' | cut -f2 -d '"')
MYSQL_ROOT_PASSWORD=$(aws secretsmanager get-secret-value --secret-id readmine-rds-creds --region us-east-1 --query SecretString --output text | jq '."password"')
REDMINE_DB_USERNAME=$(aws secretsmanager get-secret-value --secret-id readmine-rds-creds --region us-east-1 --query SecretString --output text | jq '."username"')
MYSQL_DATABASE=$(aws secretsmanager get-secret-value --secret-id readmine-rds-creds --region us-east-1 --query SecretString --output text | jq '."engine"')
REDMINE_DB_PASSWORD=$(aws secretsmanager get-secret-value --secret-id readmine-rds-creds --region us-east-1 --query SecretString --out text | jq '."password"')
REDMINE_SECRET_KEY_BASE=$(aws secretsmanager get-secret-value --secret-id readmine-rds-creds --region us-east-1 --query SecretString --output text | jq '."password"')